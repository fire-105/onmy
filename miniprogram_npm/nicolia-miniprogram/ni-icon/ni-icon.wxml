<wxs src="./ni-icon.wxs" module="computed" />
<view class="{{ computed.rootClass({ classPrefix, name }) }}" style="{{ computed.rootStyle({ customStyle, color, size }) }}" bindtap="onClick">
  <image wx:if="{{ computed.isImage(name) }}" src="{{ name }}" mode="aspectFit" class="ni-icon__image" />
</view>